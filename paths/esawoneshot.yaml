post:
  tags:
    - Create
  summary: Create Request
  description: Create the certificate request to be used
  operationId: esawoneshot
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              details:
                type: string
              pk:
                type: string  
          example:
            details: "a"
            pk: "a"
